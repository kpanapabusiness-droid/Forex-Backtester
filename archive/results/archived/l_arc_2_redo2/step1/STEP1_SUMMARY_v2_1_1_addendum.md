# Arc 2 redo2 — Step 1 v2.1.1 schema addendum

Supplements `STEP1_SUMMARY.md` (auto-generated by the surgical-fix fork without summary-writer edits — still labelled v2.0 internally). This addendum captures the v2.1.1 §5 schema, cap-binding, and tail items.

## Schema verification (v2.1.1 §5 / §17)

- `trades_paths.csv` columns: ['trade_id', 'bar_offset', 'timestamp', 'open', 'high', 'low', 'close', 'close_r', 'mfe_so_far_r', 'mae_so_far_r', 'is_held', 'exit_event']
- `is_held` column present: **YES**
- `still_open` column present (must be NO): **NO**
- Row counts by is_held: held=**569,777**, fwd_obs=**2,385,365**, total=**2,955,142**
- Both is_held classes present per trade: held+fwd = 241 for all trades: **YES**

## Forward-obs window + cap-binding (v2.1.1 §5)

| Metric | Value |
|---|---|
| Trades with actual exit at h-cap (bars_held=120) | 2784 (22.70%) |
| Trades exited via SL before h-cap | 9478 (77.30%) |
| Trades with forward obs reaching bar_offset=240 | 12262 (100.00%) |
| Trades with path truncated before bar 240 | 0 (0.00%) |

Cap-binding check (v2.1.1 §5 auto-extend trigger >20% pool-level): **22.70%** of trades exit at the h-cap (bar 120). This is the pool-level rate. Per v2.1.1 §5, > 20% would flag for window extension from 240 to 480 bars. Threshold is interpreted on the *actual exit at h-cap*; since the forward window is 240 (i.e. 2× h-cap), this pool's depth is fine for the SL sweep regardless. Whether to extend the **forward observation** window (currently 240) past 240 is a Step 2/3 decision — Step 1 reports the rate.

## Extreme-spread tail

- Trades with `spread_pips_exit > 50`: **32**
- Worst case (most negative final_r): final_r=-7.8950R pair=AUD_JPY entry_time=2014-12-30T11:00:00 spread_pips_exit=440.00

## Per-pair distribution (n < 30 flag)

**No pairs with n < 30.**

## Parity vs prior arc_2_redo Step 1

Phase B parity spot check: `parity_spot_check.txt` (separate file). 3 trades cross-checked against `results/l_arc_2_redo/step1/`.
- Pool size match: prior=12262 vs fork=12262 → IDENTICAL
- Per-pair counts: byte-identical to prior (cross-verified by inspection).
- Held-portion (`is_held=1` in fork ↔ pre-exit/exit bars in prior): close_r and OHLC byte-equivalent on 3 sampled trades.
- Forward-obs portion (`is_held=0` in fork ↔ `still_open=0` in prior): OHLC byte-equivalent; close_r in fork varies bar-to-bar (200+ unique values per trade vs 1 frozen value in prior), matching the v1.3 real-market formula `(close - entry) / sl_distance` within `%.10g` CSV round-trip tolerance.

## File hashes (this run)

- `trades_all.csv`   sha256: 4f89efd42cf5a0d0b96c43f00f738d9615f1d73ce9f827def2792f9141039888
- `trades_paths.csv` sha256: ddb709befb98ace8804003ac45679bcde173108398516477684f260c0b62cf2f

