# Phase 5 — C1 parameter sweep for c1_disparity_index. C1-only, exit=flip, fixed SL.
# Consumed by Phase 5 driver; outputs to results/phase5/c1_disparity_index/
c1_name: "c1_disparity_index"
base_config: "phase5_base.yaml"
param_grid:
  period: [14, 21, 28]
