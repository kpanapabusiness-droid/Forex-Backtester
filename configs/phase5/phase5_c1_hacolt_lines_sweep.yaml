# Phase 5 — C1 parameter sweep for c1_hacolt_lines. C1-only, exit=flip, fixed SL.
# Only combos where fast_period < slow_period are generated by the driver.
# Consumed by Phase 5 driver; outputs to results/phase5/c1_hacolt_lines/
c1_name: "c1_hacolt_lines"
base_config: "phase5_base.yaml"
param_grid:
  fast_period: [10, 14]
  slow_period: [30, 40]
