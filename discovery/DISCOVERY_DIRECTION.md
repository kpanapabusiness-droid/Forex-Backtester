# DISCOVERY_DIRECTION — Strategist explore-now MENU

> **What this is.** A grounded MENU of `explore-now` candidate scopes — directions the **current
> apparatus can build + measure today** — produced by a generative council (parallel lenses → peer
> cross-examination → chairman synthesis) over the ENTIRE honest-era corpus. Read at arc step (a)
> alongside [`LESSONS.md`](./LESSONS.md). **These are options the arc runners may CHOOSE FROM — not
> orders.** The runner picks what to test; each candidate carries its corpus citation, *because*,
> falsifiable prediction, and the most likely way it dies.
>
> **First run:** 2026-06-05 (Strategist dispatch). Operator-gated structural unlocks live separately in
> [`NEEDS_ENABLEMENT.md`](./NEEDS_ENABLEMENT.md). Methodology unchanged; this is direction, not a gate.

---

## 0. The headline the council reached (read this before the menu)

After ~70 honest-era arcs the corpus is **comprehensively closed** on single-leg directional, calendar,
microstructure, regime, and naive relative-value ground (see [`LESSONS.md`](./LESSONS.md) "Closed ground"
and the [`ESCALATION`](./ESCALATION_apparatus_capability.md)). The council's job was to find what the
single-pair search is *blind to*. Two findings dominate, and they reshape the route:

1. **The genuinely-open `explore-now` frontier is THIN and almost entirely CONDITIONAL.** Every
   unconditional single-pair cut is dead. What survives the cross-examination are a handful of
   **one-leg, cross-instrument or state-CONDITIONAL** tests that attack a *specific unconditional*
   corpus finding from an angle it never tested (esp. arc 3005's "triangulation residual ≈ 0" was
   measured **unconditionally** and at **H4 only**). These are listed below.

2. **A correction to the standing escalation (important).** The escalation ranks "a second simultaneous
   leg / netted book" as the #1 unlock for relative-value. The council's quant + capability-gap lenses,
   independently, and the adversarial cross-examiner, **downgrade this**: under FundedNext, internal
   netting saves only the spread *overlap* (~1–2 bp); commission ($5/lot) and slippage (0.5 pip/fill)
   are billed **per leg per lot regardless of netting**. Cost only truly halves when two legs collapse
   into **one quoted cross** — and that cross is *already in the closed directional ground* (a coin-flip).
   Measured relative drift on liquid majors is 2–10 bp of coin-flip. **So a netted book makes a
   coin-flip cheaper, not winning.** The genuinely cheap, high-value unlock hiding behind it is the
   **single co-simulated portfolio equity curve** (settle the existing book's gate question honestly) —
   see [`NEEDS_ENABLEMENT.md`](./NEEDS_ENABLEMENT.md) item E. This corroborates arc 2019: *edge-hunting
   for the existing 4-component book is closed*; the lever is the operator's gate-governance call, not a
   5th reversion leg.

**Implication for runners:** the menu below is real but **lower-probability than a fresh frontier would
be** — the corpus is mature. Prefer the candidates that attack a *named unconditional/un-tested* corpus
result (M1, L1, O1). Treat the long-shots as cheap observations, not campaigns. If a candidate dies at
the cheap-obs stage, that is a *high-value closure*, not a wasted arc.

---

## How the council reasoned (the trail, condensed)

Five generative lenses ran in parallel, then cross-examined each other:

- **Frame-breaker** named the deepest shared trap: **predictive monism** — every arc tries to *win by
  being right about price* and pays a toll to express the forecast; none tries to *be paid to be present*
  (liquidity supply / premium collection). Its proposals: short-gamma fade, dispersion-state, trigger
  shallowing, passive-fill cost-collection.
- **Capability-gap** ranked the expressibility frontier and did the decisive arithmetic showing the 4
  survivors **do not net** (disjoint event-timing ≠ simultaneous offset; me_long/me_short fire on
  different instruments/months) — so the netted book re-lights *zero* existing components. Its real
  finding: the **co-simulated equity curve** is the cheap, decisive build.
- **Practitioner** injected real-desk classes: dollar-factor residual, cointegrated *baskets* (vs naive
  pairs), carry **risk-premium** (spot, not income), COT positioning, post-macro-release reversion.
- **Quant/relational** did the cross-instrument math: the triangulation residual was killed only at the
  **level** and only at **H4** — its **second moment** and its **driver-shock-conditional** first moment
  are virgin ground, and both are **one-leg expressible** (no netted engine).
- **Divergent** attacked the unexamined axes: the bid-ask **spread as a signal** (not a cost), forced-flow
  **density** as a meta-filter, a **new forced actor** (central-bank peg defense — a non-reverting join),
  and the engine's own **take-the-loss tax** as an entry-geometry filter.

The **adversarial cross-examination** then collapsed the weak ones: short-gamma fade and vol-compression
breakout → dead mean-reversion/breakout in costume; dollar-factor / dispersion / carry-spot / correlation-
break → relative-value coin-flip (and the multi-leg ones are Arc-10 cost traps); Tokyo-fix → the H1 cost
wall; passive-fill → the canonical Arc-10 fabrication unless adverse selection is modeled. The survivors
that cleared scrutiny are below. **Several survivors are collinear** — O1 (forced-flow density), the
spread-z timer, and trigger-shallowing are three proxies of *one latent "inelasticity-state" variable*;
build it once.

---

## The MENU (explore-now — choose from these)

Ranked by the council's expected value. Each is buildable on the current apparatus (single-pair / both
directions, H1/H4/D1/W1, 28 pairs, masks + ATR geometry, the 6 SL-honest exit policies, the per-fold
combiner). None collapses into closed ground *as scoped*. Hold each to the same rigor as an arc.

### M1 — Driver-shock-CONDITIONAL cross-timeframe residual (the freshest prior) · `explore-now`

- **Corpus citation.** Arc 3005 killed the cross-rate triangulation residual but **only unconditionally
  and only at H4** (fwd-convergence corr ≈ 0.01). Single-pair MTF is closed (TF-invariant) — but a
  cross-instrument lead-lag linked by the triangular identity was never tested.
- **Because.** `EURJPY ≡ EURUSD × USDJPY` is an identity. A large directional shock in a **driver leg**
  (e.g. EURUSD on the D1 close) *forces* the dependent cross to move; if the JPY market is in a quiet
  session, the quoted cross may re-price with a lag at the next H4 bar even though the *same-bar* H4
  residual is ≈ 0 on average. You are not forecasting price — you are riding a transient identity
  dislocation created by asynchronous re-pricing.
- **Falsifiable prediction.** Condition on `|EURUSD D1 return| > 1.5·ATR`; measure the **next-H4-bar**
  EURJPY residual (the part not explained by contemporaneous USDJPY). If real: `corr(driver_shock,
  next_bar_cross_residual) > 0.05` — materially above arc 3005's unconditional ≈ 0.01. **Falsifier:** if
  conditioning does not lift it above ~0.05, the cross re-prices within the bar (efficient) → dead.
- **Expression.** **One leg** — trade the cross; the driver leg is a *signal*, not a position. Cost ~0.8 bp
  (cross spread). No netted engine.
- **Most likely death.** Triangular arb is the most-policed FX relationship and H4 is slow; the honest
  prior is FAIL. **And** the conditioning produces a thin event set, so OOS-fold discipline must be
  ruthless (cf. the shock-continuation epoch-dependence death, arc 3019). Run the cheap conditional-corr
  observation FIRST; only build the leg if it clears ~0.05.

### O1 — Inelasticity-STATE conditioning of the surviving edges (build the latent variable ONCE) · `explore-now`

- **Corpus citation.** The 4 survivors (gap-fill, me_long, me_short, fbr) are mean-positive but **too thin
  to resolve folds** (arcs 2016/2017/2019/1023). The only surviving mechanism is *forced flow*; the only
  surviving calendar driver is *month-end* (arc 3015). The bid-ask spread series exists in the panel but
  has only ever been used as a **cost**, never as a **signal**.
- **Because.** Forced flows are not Poisson — they pile onto a handful of windows (month-end, quarter-end,
  fiscal-year-end, fixings) and onto bars where the book is **inelastic** (a flow moves price more per unit
  when depth is thin; the subsequent reversion is then larger). A wide bid-ask spread at the dislocation
  bar is a lag-free proxy for "how inelastic was the book." **The survivors infer inelasticity only from
  the calendar; the spread (and a forced-flow "density" score) are orthogonal proxies of the same latent
  state.** Concentrating risk into high-inelasticity bars raises edge-per-trade without adding coin-flip
  trades — a direct attack on the fold-resolution problem.
- **Falsifiable prediction.** Define a single **inelasticity-state** variable (proxies: forced-flow
  calendar-density score; entry-bar spread-z; range-compression-then-expansion). Within the existing
  fbr / month-end edges, per-trade realized R should rise **monotonically** with the state variable, and a
  book restricted to its top decile should have a **higher worst-fold ROI** than the unconditioned edge.
- **Expression.** A mask over existing edges + the existing combiner. No new data.
- **Collinearity note (load-bearing).** Forced-flow-density, spread-z, and "shallow the fbr/month-end
  trigger" are **the same variable in three costumes** — build the state variable ONCE with multiple
  proxies; do not spend three arcs on it.
- **Most likely death.** (1) Smaller sample → *worse* fold resolution if edge-concentration isn't steep
  enough (trading one thinness problem for another). (2) **Cost trap on the spread-z proxy:** wide-spread
  bars are exactly where you pay the most — any test MUST charge the *realized wide spread* at entry, or it
  is an Arc-10 cousin (fabricated optimism). (3) fbr specifically **does not thicken** (arc 1025: trigger
  depth and edge strength are coupled) — so the *shallowing* proxy is the weakest; expect it to confirm
  1025. The calendar-density and honestly-costed spread-z proxies are the live ones.

### L1 — Triangulation residual SECOND MOMENT (OU amplitude) at finer resolution · `explore-now`

- **Corpus citation.** Arc 3005 killed the residual **level** (median ≈ 0, fwd-convergence corr ≈ 0.01)
  but reported a non-trivial **residual std 1.23–1.58 bp** and |residual| > spread on 1.3–6.1% of H4 bars
  — and explicitly flagged M1/sub-H4 as **"out of apparatus scope, a different cost regime."** That flag
  is the untested door. The residual's *variance* was never traded.
- **Because.** Triangular arb pins the residual *mean* to ≈ 0, but the residual is the difference of three
  asynchronously-updating quotes; its **amplitude** spikes when one leg is temporarily stale (session
  handovers, one-currency news, fix windows). The residual behaves as a mean-reverting OU process around
  0 — you harvest the *amplitude* (a convergence trade), not the direction.
- **Falsifiable prediction.** Fit `dr = −θ·r·dt + σ·dW` to the residual at H4 → H1 → M15 → M1. The
  mean-reversion **half-life should shorten** as resolution rises; find the resolution where half-life
  < ~5 bars AND the OU amplitude σ exceeds the single-cross cost on > ~5% of bars. Trade: `|residual_z| > 2`
  → take the convergence (cheap) side, exit on convergence. **Falsifier:** if at *every* resolution down
  to M1 the fraction of bars where amplitude > capture-cost stays below ~5%, the triangulation thread is
  *fully* closed (level AND variance).
- **Expression.** **One leg** — trade the single quoted cross when it's cheap vs its synthetic reference
  (the synthetic is a reference, not a held position). No netted engine. Needs M1→H1 residual computation
  (the M1 bid/ask data exists).
- **Most likely death.** Knife-edge: ~1.3 bp amplitude vs ~1 bp cost is inside the noise band, and going
  to H1/M1 walks straight into the **H1 cost wall (~2× H4)** that killed the whole microstructure cluster
  (gotobi / round-number / WMR-fix / session-break). Also: a one-leg expression reintroduces directional
  exposure between entry and convergence. Expect cost to eat it — but the variance dimension is genuinely
  virgin, so a cheap multi-resolution observation is worth it to close the door cleanly.

### S1 — Take-the-loss-TAX entry geometry (a MODIFIER on M1/O1, not a standalone) · `explore-now`

- **Corpus citation.** The CI-locked invariant: a stop breach at/before the +1R partial books −1R;
  same-bar = SL-first; ambiguity never wins. fbr (arc 1013) is *literally* an instance of "reclaim, then
  enter, stop beyond the swept extreme" — favorable-move-first geometry — and it is the corpus's cleanest
  edge. The principle was never generalized.
- **Because.** The −1R tax punishes paths that touch the stop *before* the +1R partial. Entering a
  forced-flow reversion **after the reclaim prints, with the stop beyond the dislocation extreme**, makes
  the path "favorable-first," so the tax falls preferentially on the *coin-flip noise* trades you don't
  want — the engine's honest pessimism becomes a free path-quality filter.
- **Falsifiable prediction.** Partition entries by timing relative to the dislocation extreme
  (enter-on-touch vs enter-on-reclaim, stop-beyond-extreme). The reclaim geometry should reach
  +1R-partial-before-stop on a **higher fraction** of trades, by a margin **larger** than the difference
  in their stop-agnostic edge (i.e. the booked − theoretical gap shrinks).
- **Most likely death.** Probably **tautological** — fbr already exists *because* this works; applying it
  to fixings (which have no single "extreme" to place a stop beyond) may not generalize. Also risks
  **same-bar reclaim lookahead** (the reclaim must be detected without using the entry bar's future). Use
  as a refinement multiplier on M1/O1 survivors, not as its own arc.

### Q1 — Central-bank PEG / boundary-defense persistence (the long-shot; a NEW forced actor) · `explore-now` to detect

- **Corpus citation.** The 4 survivors are all *reversions of an over-extension*; the divergent seat asked
  for the inverse — a forced flow that creates a **persistent, non-reverting** move you can join. No arc
  has tested a price-insensitive actor *defending a level* (vs a one-shot dislocation).
- **Because.** A central bank defending a band (historically EURCHF 1.20 floor, USDHKD 7.75/7.85, CNH soft
  management, managed EM floats) is a forced, infinite-balance-sheet, *repeated* counterparty pinning price
  to one side. The OHLC footprint is unmistakable: realized vol collapses and the price distribution
  **truncates** against the boundary. You fade touches of the defended boundary repeatedly — many trades
  per year, which is the exact *thickness* the survivor book lacks.
- **Falsifiable prediction.** Detect "boundary regimes" from OHLC alone (rolling realized vol below its
  X-th percentile + a truncated distribution near a round level). Fading boundary touches *inside* such
  regimes shows high win-rate / low-variance positive R; *outside* them the same rule is a coin-flip. The
  edge appears/disappears **with the regime**, not smoothly.
- **Most likely death.** (1) **Epoch-dependent** (cf. shock-continuation, arc 3019) — too few defended-band
  pair-years in 2010–2021+ to populate every calendar-year fold → fails the per-year gate by *absence of
  regime*, not by being wrong. (2) **Fat left tail:** a peg *break* (CHF Jan-2015) gaps *through* the stop;
  the −1R invariant caps per-trade but a break can blow the daily-DD cap across the book → **must be a
  small, tail-aware, regime-flagged probe; never sized normally.** Marked as a long-shot deliberately
  (preserve the spread) — its asymmetry (a genuinely thick edge if it exists) justifies a cheap detection
  pass even though it will probably die on epoch/absence.

### G1 — Dollar-factor beta-residual reversion (MARGINAL; run only as a kill-test) · `explore-now`, adjacent to closed ground

- **Corpus citation.** Cross-sectional USD-neutral month-end rebalancing was tested (arc 2018) and found
  coin-flip + sub-cost, robustly negative in 2018; relative-value momentum is coin-flip (arc 2003).
- **Because (the claim).** Decompose each USD-major into a common-dollar factor (cross-sectional mean of
  the 7 USD pairs) + an idiosyncratic residual via rolling beta; an index/basket rebalancer trades the
  *dollar*, leaving an over/under-shot *name* residual to revert.
- **Falsifiable prediction / why it's only a kill-test.** The cross-examiner's decisive objection:
  near-daily firing is the *tell* that this is coin-flip noise, not forced flow — forced flow bites at
  month-end, where this **is** arc 2018 (already dead). Keep ONLY the discriminating observation: is
  residual-reversion *strictly stronger* on the largest dollar-factor-move days **and** inside forced-flow
  windows? If not monotone in dollar-move magnitude, it is disguised relative-value → confirm dead. Do not
  open as a campaign; run the one cheap kill-test and close it.

---

## What the council explicitly does NOT recommend as a route to a PASS

- **A 5th decorrelated REVERSION leg for the existing book.** Arc 2019 proved it cannot make the book
  all-folds-positive (no diversification deficit, no tail to hedge; an added thin leg just adds another
  every-year trip-chance). The book's failure is **gate-resolution below the noise floor** — an operator
  governance call, not an edge hunt.
- **A vol-target / risk-parity overlay to "raise the worst fold."** This finds no edge; if it uses any
  contemporaneous knowledge of which years are bad it is lookahead (fabricated worst-fold lift); ex-ante
  it merely scales a fold, it cannot turn a negative-expectancy fold positive. Acceptable only as a
  neutral ex-ante sizing default *after* an edge exists — never as a path to PASS. The min-over-years gate
  question belongs to the operator (arc 2019).
- **Anything in the CLOSED GROUND** ([`LESSONS.md`](./LESSONS.md)): shallow direction (long/short, every
  TF/pair), regime conditioning, naive relative-value / cross-sectional pairs, H1 microstructure flow,
  sub-month-end calendar, convexity/trend, carry income, option/gamma (no strike data).

---

## Pointer

Operator-gated structural unlocks (the co-simulated equity curve; the netted-book caveat; passive-fill
with adverse selection; COT / calendar / options / macro data; execution-venue) are ranked in
[`NEEDS_ENABLEMENT.md`](./NEEDS_ENABLEMENT.md). The single highest-ROI build there (the **co-simulated
equity curve**, item E) settles the existing book's gate question honestly and can only *tighten* the
verdict — zero fabrication surface. It is the council's recommended next build, ahead of any new edge.
