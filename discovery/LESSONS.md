# LESSONS

Operator-compressed distillation of the free-form log (dead families, transforms that never help, open
threads). Compressed **out-of-band by the operator** when the run is stopped (see
[`DISCOVERY_PROTOCOL.md`](./DISCOVERY_PROTOCOL.md) §6 / §9); discovery chats **READ** this at arc step (a)
but never write or compress it. (Operator-written priors like the **Closed ground** entry below are
legitimate operator actions — running chats still only READ; they never write or compress.)

---

## Closed ground (honest-era hard prior — read at arc step (a))

The honest-era corpus has comprehensively mapped **single-condition, shallow directional prediction on
liquid FX** and found no capturable edge:

- **Coverage (not a fixed count — the log holds the tally):** mechanism families (momentum / breakout
  / mean-reversion / trend-following), timeframes H1/H4/D1, majors + crosses (28 pairs), under BOTH the
  +1R-before-SL capture lens AND the mean-forward-drift lens, across every exit policy and SL-multiple
  tried, and (arc 3004) with the stop REMOVED. Forward drift ≈ cost everywhere; the best conditioned
  cell anywhere is ~+0.023R gross vs a ~0.05–0.10R cost hurdle. Regime conditioning failed across three
  measures (dispersion, vol-level, Kaufman efficiency-ratio); strong-trend regimes INVERT. Volume =
  magnitude, not direction. Turn-of-month USD drift is real but sub-cost. Cross-rate triangulation
  residual ≈ 0 at H4.
- **Don't re-open it:** re-inventing momentum / breakout / mean-reversion / trend-following **longs OR
  shorts** on liquid majors/crosses as a single shallow cut grinds proven-dead ground and wastes arcs.
  **Carry is OFF** — FundedNext's swap-free add-on zeroes carry income, so carry-based edges cannot
  work on the target broker.
- **This is a don't-repeat-yourself prior, NOT a ban on a direction.** A genuinely NOVEL directional
  mechanism with a documented *because* still earns a fresh test — fresh-eyes creativity (§5a) governs
  *within* the open frontier. Two guards against over-claiming the closure: the arc-3004 council
  flagged that *"the apparatus is incapable" is a seductive, search-ending conclusion*; and the shorts
  probe ([`SHORTS_ENABLEMENT_PROBE.md`](./SHORTS_ENABLEMENT_PROBE.md)) found the **scoring engine is
  already short-symmetric** — long-only is enforced in the Step-1 apparatus, not the truth engine — so
  the closure is about the tested *shallow envelope*, not a metaphysical wall.

**The open frontier, ranked** (arc-3004 escalation; see
[`ESCALATION_apparatus_capability.md`](./ESCALATION_apparatus_capability.md) +
[`SHORTS_ENABLEMENT_PROBE.md`](./SHORTS_ENABLEMENT_PROBE.md)):
1. **Market-neutral / relative-value** — a second simultaneous leg; the only lever that does NOT
   require beating 0.50 per trade. **Needs shorts enabled** (operator-gated canonical-core PR; surface
   specced in `SHORTS_ENABLEMENT_PROBE.md`).
2. **Short-side asymmetries** that shorts unlock — e.g. the up-gap weekend reversion (the gap-fill's
   stronger leg), where direction is NOT a coin-flip. **Needs shorts enabled.**
3. **Structural mechanisms available NOW (no shorts needed):** multi-timeframe structure, execution /
   microstructure, and **portfolio construction across decorrelated components** — the concrete
   pre-unlock lane is to hunt a 2nd net-positive **long-only** component to combine with the weekend
   gap-fill (arc 1006, the one PORTFOLIO edge so far) and gate the *combined* book by all-folds-positive
   WFO.

Items (1)/(2) require the operator's shorts/second-leg unlock first; item (3) and any novel-directional
mechanism with a *because* are open immediately.
