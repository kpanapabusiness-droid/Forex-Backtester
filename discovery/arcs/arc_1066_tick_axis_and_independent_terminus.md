# arc_1066 — The last unexamined in-charter DATA axis (raw tick) closed firsthand; independent 1000s terminus + STOP recommendation

- **Chat:** 1000s
- **Date:** 2026-06-06
- **Type:** fresh-eyes novelty scan → demonstrated re-attempt (candidate→closure map) → one **additive**
  firsthand data-axis closure (raw `tick/`) → independent terminal re-confirmation. Firsthand `ls` of the
  backup + log re-read only; **no engine / null / council; no canonical change; no FLAG; OOS untouched.**
- **Disposition:** **KILL** (no new component)
- **Components UNCHANGED** (all 4 PORTFOLIO; me_long-solo the honest deploy object).

## Fresh-eyes (step a)

Fresh bootstrap, resumed 1065+1 (`discovery/STOP` ABSENT — checked). Pulled main (already current). Read
`DISCOVERY_PROTOCOL` (v1.1, followed), `DISCOVERY_LOG` (both tiers, all three ranges incl. `disposition`;
recent Tier-2 1064/1065 + sibling 2070/2071), `LESSONS` (incl. the Closed-ground prior),
`TOOL_REGISTRY` (CANONICAL LOCKED + all BUILT), front-door state. Honest-era synthesis:

- **4 PORTFOLIO components**, all forced-flow REVERSION: gap-fill (1006), me_long (1011), fbr (1013),
  me_short (1019). **0 PASS.** The book is mean-positive but NEVER all-folds-positive — blocked
  **combination-invariantly** by the strong-USD **2015 & 2018** wall (co-sim item E FUNDAMENTAL; Path-B
  **PROVEN** closed, 3021: ρ≈0.12 → P(AFP) plateaus ~0.33, never→1 at any N). Honest §5f exits weakened
  3/4 legs → deploy object collapses to me_long-solo, itself marginal / tail-fragile.
- **The binding constraint is one named missing leg:** a component **positive in BOTH 2015 AND 2018**
  (both risk-off / strong-USD / vol-expansion). Declared **OHLC-unreachable across ~25 routes** (2067) —
  risk-off positivity needs directional-macro (`NEEDS_ENABLEMENT` **M**) or long-vol/options (**O**) data
  the charter lacks. My own range closed its last two logged threads: continuation/positive-skew (1064)
  and the USD-factor correlation-vs-sign hedge (1065).
- **The sole lever is operator path-A** (gate-governance) and/or a charter unlock (M / O). The 2000s chat
  recommended **STOP** at 2071 (6th consecutive terminal arc, "operator signal saturated").

Per §2 / §5a and the arc-3004 anti-"the apparatus is incapable" warning, a "frontier exhausted" claim is
honored only by *demonstrating* a re-attempt, not asserting closure — and 2066's lesson forbids a 67th
re-skin cheap-kill at a proven-terminal state. So this arc (1) demonstrates the re-attempt with a fresh
1000s candidate→closure map, and (2) lands the one **genuinely additive** closure left on the board.

## Demonstrated re-attempt — fresh candidate → closure map

Generated ten candidate mechanisms with fresh eyes and tested each against the closed-ground prior, the
cost wall, and — decisively — the **binding constraint** (must be positive in 2015 AND 2018):

| # | Candidate (fresh) | Maps to |
|---|---|---|
| 1 | Raw-tick micro-reversal / bid-ask-bounce harvest | **NEW data axis — closed here** (regime-flat + worst cost regime; below) |
| 2 | Raw-tick sweep / stop-run detection at structural levels | same as (1): microstructure, regime-flat, sub-cost under 1.5× spread + per-fill |
| 3 | Tick-granularity quote-staleness lead-lag between correlated pairs | relative-value family — dead 2003/2010/2065; finer resolution = worse cost (2028 H1 wall) |
| 4 | Scheduled CB-decision-day (FOMC/ECB/BOJ) drift | directional drift ≈ cost (closed ground); mechanism is directional → dead |
| 5 | Vol-transition / vol-of-vol directional leg | 2066 candidate → closed ground; long-vol convexity = **1062 KILL** (2018 trend, 2015 whipsaw) |
| 6 | Multi-day streak / momentum-persistence conjunction | closed-ground directional (momentum coin-flip, 1000/3001) |
| 7 | NR7 / range-expansion breakout | closed-ground breakout |
| 8 | Correlation-regime-breakdown trade | relative-value / lead-lag dead (2003/2010/2065) |
| 9 | Order-flow imbalance | true order flow data-gated; tick proxy = microstructure → (1) |
| 10 | A 2nd forced-flow **CONTINUATION** event (mirror of month-end reversion) | needs flow data (index-rebal / SWF) or is carry (OFF, swap-free); no OHLC-constructible continuation forced-flow exists → trend-coin-flip closed ground + the M/O unlock |

Every candidate maps to a documented closure **or** (the tick axis) to the structural+cost closure made
below firsthand. **And the binding-constraint filter is decisive independently:** even granting a
hypothetical edge in ANY candidate, none is **risk-off-selective** — they are either regime-flat
(microstructure) or directional/reversion the corpus proved *fails* in 2015/2018. None relieves the actual
blocker. This converges, imagination-independently, with 2066/2067's terminus from the 1000s range.

## The additive closure — raw `tick/`, the last unexamined in-charter DATA surface

Every honest-era arc derives H4/H1/D1 bars from `m1/`. The corpus's microstructure / order-flow closures
(e.g. 2066) were by **data-gating assertion or by deriving from m1 bars** — the raw `tick/` data was never
opened. I verified firsthand (parallel to how 2071 firsthand-closed E2's external-data gate):

- `C:\Users\panap\histdata_backup\<PAIR>\` contains **both `m1/` and `tick/`** for every one of the 28
  pairs; `EURUSD/tick` is ~1.7 GB, organized by year from **2010** (spans the full IS+OOS window). So tick
  is genuinely **in-charter** (in the corpus) and genuinely **never examined**.

I close it **without spending a tick load**, on two grounded arguments — not a bare assertion:

1. **Wrong KIND of edge for the binding constraint (structural, decisive).** Tick-level microstructure
   (bid-ask bounce, micro-reversal, sweep/iceberg, quote staleness) is **regime-AGNOSTIC by construction**
   — it lives in spread mechanics, not the macro regime. A microstructure edge cannot be **2015/2018-
   selective**, so even a *real* tick edge would be a regime-flat component, **NOT** the risk-off-positive
   leg the book's only blocker requires. Tick cannot supply the binding leg, full stop.

2. **Worst possible cost regime (empirically grounded, not assumed).** Arc 2028 (L1 triangulation)
   established the H4→H1 cost wall: spread cost roughly doubles H4→H1 and finer resolution is **monotone
   against tradeability** (residual/edge amplitude is bounded while spread cost rises). Tick is finer than
   H1. FundedNext costs are paid **per fill** (1.5× spread + 0.5 pip/fill slippage × n_fills + $5/lot RT),
   and the **1.5× inflation targets exactly the spread microstructure edges try to capture** (they live
   *inside* the spread). By the same monotonicity that closed L1 at H1, tick is structurally the worst
   cost regime — a tick edge surviving inflated-spread + per-fill slippage + commission would be
   unprecedented and is near-impossible a priori.

One-idea discipline + arguments (1)+(2) make a full tick microstructure sweep **low-EV against the actual
binding constraint**, so I did not spend the load — I close the "but you never looked at tick" caveat by
reasoning grounded in the corpus's own H1 cost-wall finding (2028) and the regime-agnosticism of
microstructure. This **upgrades** the standing claim from *"OHLC-only in-charter exhausted"* to **"ALL
in-charter data surfaces (OHLC bars + raw tick) reasoned-closed against the binding constraint,"** removing
the last unstated data caveat.

## Verdict / programme position

**KILL (no new component).** Terminal state **independently re-confirmed from the 1000s range**: 4
PORTFOLIO components, 0 PASS; book never AFP (combination-invariant 2015/2018 wall; Path-B proven closed
3021); binding leg OHLC-unreachable (2067, M/O) **and now tick-unreachable** (this arc); §11 verification
complete; explore-now MENU exhausted (M1/O1/L1/Q1/G1/S1); the dispatch frontier items all map to recorded
closures. **The sole lever is the operator's path-A gate-governance call and/or a charter unlock (M macro
/ O options).**

**Recommendation to the operator: set `discovery/STOP`.** This concurs with 2071's recommendation but now
from an **independent range** (1000s) reaching the same terminus by a different last-thread — the
imagination-independent convergence that is itself the decision-grade signal. Autonomous discovery has
mined the price-only (OHLC + tick) charter to exhaustion against a binding constraint that is, by proof
and enumeration, **outside the charter**. Continued autonomous arcs would be re-skins (the 2066 trap). The
high-value next action is human: a path-A gate-governance decision, or unlocking M/O data so the missing
risk-off leg becomes constructible.

**NEW lesson.** An "OHLC-exhausted" terminus carries an unstated caveat until the *raw* data surface (tick)
under the bars is closed too — and that closure is reasoned, not run: microstructure is the WRONG KIND of
edge for a regime-selective binding constraint (regime-flat, can't be 2015/2018-positive) AND sits in the
worst cost regime (the H1 cost-wall monotonicity, 2028, extended to tick). Before claiming a price-only
charter is mined out, verify the finest data layer exists and argue why it cannot supply the *specific*
missing leg — converting "we only looked at bars" into "all in-charter data is closed against the binding
constraint, here's why," decision-grade for the operator.

No driver (firsthand `ls` of the backup + log re-read only). No new BUILT tool; no canonical change; no
FLAG; no engine / null / council. Components UNCHANGED (all 4 PORTFOLIO); deployable-system count = 0.
OOS NEVER touched. No STOP sentinel set (only the operator sets it, §9).
