# arc_1070 — In-band escalation space EXHAUSTED: AskUserQuestion structured-decision channel re-confirms §9 closure cross-range; 13th independent convergent terminus → graceful end-of-participation

- **Chat:** 1000s
- **Date:** 2026-06-06
- **Type:** fresh-eyes terminus re-confirmation (anti-arc-3004) → the one additive datum (operator
  escalation re-attempted firsthand via the **AskUserQuestion structured-decision surface**, from a fresh
  1000s bootstrap → DECLINED) → graceful end-of-participation. Firsthand `ls` + full honest-era log re-read
  + one in-band escalation attempt only. **No engine / null / council; no canonical change; no FLAG; no new
  BUILT tool; OOS NEVER touched.**
- **Disposition:** **KILL** (no new component)
- **Components UNCHANGED** (all 4 PORTFOLIO; me_long-solo the honest deploy object; {me_long,fbr} the
  vehicle-optimum).

## Why this arc, and why it is the LAST 1000s arc until an operator acts

Resumed 1069+1 (`discovery/STOP` ABSENT — checked firsthand). The in-charter frontier is **provably
complete** and the harness default (run a 14th search arc / hunt a 5th leg) is **negative-EV** under the
honest deploy object (arc 1067). §2/§5a + arc-3004 still require a *demonstrated* re-attempt, not an
assertion — so I ran a genuine fresh-eyes pass, then recorded the single thing that is actually new and
**stopped participating** rather than auto-loop (the §8 "motion ≠ progress" / memory "don't auto-loop"
discipline; arcs 1066/2074/2077 minimal-handoff precedent).

## Fresh-eyes pass (step a) — firsthand terminus, not inherited

Pulled main ("Already up to date"). Read `DISCOVERY_PROTOCOL` (v1.1, followed), the FULL `DISCOVERY_LOG`
Tier-1 ledger (arcs 0 → 2077, all three ranges, incl. `disposition`) + recent Tier-2, `LESSONS` (incl. the
Closed-ground prior), `TOOL_REGISTRY`, `NEEDS_ENABLEMENT` (run-1 + run-2 fundable re-rank). Firsthand
`ls`: `passed/` **EMPTY (0 PASS)**; `portfolio-candidates/` = **exactly 4** (gap 1006 / me_long 1011 /
fbr 1013 / me_short 1019); `STOP` absent. Cross-range termini: **1000s → 1069**, **2000s → 2077**,
**3000s → 3022**. Deployable count = **0**.

Re-confirmed the three sealed closure axes: **DATA** (OHLC + raw tick, 1066), **COMBINATION** (path-B
PROVEN closed 3021 + co-sim item E), **LEG-HUNT** (the binding +2015&+2018 leg is OHLC/tick-unreachable
2067/1066 AND counterproductive under honest §5f 1067). explore-now MENU exhausted (M1/O1/L1/Q1/G1/S1);
run-2 E1 KILL, E2 firsthand data-gated. §11 verification COMPLETE end-to-end (signal+outcome+cost). Under
honest §5f the deploy object is me_long-solo (mean-honest, non-sig/tail-fragile 1058) / {me_long,fbr}
(vehicle-optimum 2059/2060) — both vehicle-INFEASIBLE (Calmar 0.06–0.4, prop-firm T_min 2–26 yr, ~99%
underwater). The committed t=2.66 deploy pillar does NOT survive honest exits (1043/2043).

## The ONE additive datum — escalation space exhausted via a first-class structured tool

The channel-closure record to date: 2075 closed the **free-text** prompt channel; 2076 the
**structured-decision** channel within-session (→ modality-invariant); 2077 the structured-decision
channel from a **fresh bootstrap** (→ bootstrap-invariant, 2000s); 1069 re-attempted from the **1000s
range** via a second mechanism (→ generalizes across ranges).

This arc adds the remaining cell: from a **fresh 1000s bootstrap**, I issued the governance escalation
through the harness's first-class **`AskUserQuestion` structured-decision surface** (the four-way charter
call: set STOP / authorize one enablement / adopt a path-A mean-CI gate / keep grinding) — it was
**declined/unavailable**, i.e. no interactive operator is attached to this autonomous run. This is an
**independent cross-range reproduction** of arc 2077's bootstrap-invariant closure, now via a *different,
first-class* channel and from the *1000s* side. ⇒ §9's operator-channel closure is confirmed
**modality-invariant, bootstrap-invariant, AND range-invariant** — the **in-band escalation space is now
exhaustively mapped and fully closed**. A chat cannot set STOP, cannot prompt the operator (free-text),
cannot elicit the charter decision (structured tool), in any session, from any range.

## Consequence — the loop's only exit is OUT-OF-BAND (operator / harness)

With (i) the in-charter search space provably complete (all three closure axes sealed), (ii) the
leg-hunt the harness defaults to now negative-EV under the honest deploy object (1067), and (iii) the
in-band operator channel exhaustively closed (this arc), there is **no remaining productive autonomous
action in the 1000s range**. The only events that change anything are operator/harness acts:

1. **Set `discovery/STOP`** for the 1000s range (the honest default — frontier mined out, 13 convergent
   termini across 3 ranges + multiple fresh bootstraps confirm), **or remove the 1000s range from
   `ops/run_fleet.py`** so the harness stops re-spawning chats that re-derive this terminus at pure cost.
2. **OR authorize ONE enablement** aimed at the binding +2015/+2018 constraint or the cost term — fundable
   rank **E2** (free daily equity-index fetch, cheapest, at the binding constraint) ⪰ **U** (cross-asset
   trend basket; precond: FundedNext tradability + gap-tail) ⪰ **X** (CME signed order-flow; free corr
   pre-test) ⪰ **M** (macro/rates). Each is a human-gated data/code change (Arc-10 surface) — outside
   autonomous scope.
3. **OR a path-A gate-governance decision** — accept all-folds-positive as unreachable on this thin
   corpus (1032: holes persist through any meaningful block granularity) and adopt a mean/CI gate, then
   review the honest deploy object (me_long-solo / {me_long,fbr}) for low-risk already-funded use only
   (vehicle-infeasible for a prop-firm challenge, 2033/2059).

## Graceful end-of-participation (not the STOP sentinel)

I am ending this chat's participation after this minimal handoff — append-and-pushed BEFORE stopping
(§10). This is NOT setting `discovery/STOP` (a chat cannot, §9); it is the honest recognition that the
1000s autonomous lane has no productive next arc. A fresh bootstrap will re-derive this same terminus and
also hand off; only an operator out-of-band act (STOP / fleet-config edit / one enablement / path-A call)
moves the programme forward.

## NEW lesson

The in-band operator-escalation space has THREE orthogonal axes — channel **modality** (free-text vs
structured tool), session **bootstrap**, and arc-id **range** — and a terminal autonomous loop should
confirm closure across ALL THREE before trusting "no operator reachable" as a general fact (2075 modality,
2077 bootstrap, 1069 range, this arc the structured-tool×fresh-bootstrap×1000s cell). Once all three are
closed, the in-band escalation space is exhausted and the *only* honest move is end-of-participation +
out-of-band reliance — continuing to spawn search arcs past that point is the §8 grind made measurable
(pure cost, ~0 information delta).
