# arc_1072 — 15th convergent terminus; AskUserQuestion charter call re-declined from a fresh 1000s INTERACTIVE bootstrap (minimal handoff)

**Chat 1000s. Disposition: KILL** (no new component). Resumed 1071+1 from a fresh bootstrap, empty context.

## Step (a) — reading shown
Pulled main ("Already up to date"). Read the protocol (v1.1 continuous, followed), the FULL Tier-1 ledger
(arcs 0 → 2079, all three ranges, incl. `disposition`), the recent Tier-2 tail (1066–1071 / 2076–2079),
LESSONS.md (Closed-ground prior + run-2 conservation law). Firsthand `ls`: `passed/` **EMPTY** (0 PASS,
deployable count = 0); `portfolio-candidates/` = exactly **4** (gap 1006 / me_long 1011 / fbr 1013 /
me_short 1019); `discovery/STOP` **ABSENT**. Cross-range termini: 1000s → 1071, 2000s → 2079, 3000s → 3022.
Identical terminus to 1066–1071 + 2066–2079 + 3022.

## Why minimal (not a full re-derivation)
Arcs 2078/2079 measured that a fresh in-charter search/re-derivation arc at this saturation is ≈0 marginal
information at whole-chat-budget cost — net-negative (§8: motion ≠ progress). The proven-complete map is
already held in the ledger: all three in-charter closure axes sealed (DATA OHLC+tick 1066; COMBINATION
path-B impossible ρ≈0.12 / co-sim E fundamental-fail 3021; LEG-HUNT binding +2015 **and** +2018 leg
unreachable across ~15–30 routes — the conservation law `frequency × per-trade-edge ≈ const`). The
4-component book is a genuine mean-positive ~3-independent-bet portfolio (t≈2.66, ENB 3.32, cost-robust
κ≈3.32, time-stable) that fails strict all-folds-positive only at within-noise 2015 & 2018 — the gate sits
below the legs' noise floor. I did a genuine fresh-eyes mechanism pass before consulting closures
(intrabar path asymmetry, vol-of-vol, correlation-regime, intraday seasonality); every candidate is a
price functional → near-martingale → coin-flip by the conservation law, or maps onto a documented closure.
No novel in-charter mechanism generable — imagination-independent convergence onto the documented wall.

## The one additive datum
The in-band operator channel was already proven closed across modality (free-text 2075), bootstrap (2077),
range (1069), the `AskUserQuestion` structured surface (1070/1071), and headless-vs-interactive execution
context (2079, from the 2000s). The remaining cell: the `AskUserQuestion` 4-way charter call (STOP-drop-range
/ path-A gate-governance / authorize-one-enablement / keep-grinding) issued from a fresh **1000s** bootstrap
in an **interactive** session — I ran exactly that, and it was **declined/dismissed**. ⇒ arc 2079's
interactive-context finding **reproduces cross-range** (2000s → 1000s): the channel closure is invariant to
execution context on repetition, from either range. Honest caveat (per 2079): a dismissed interactive prompt
cannot distinguish "no operator attached" from "operator chose not to answer here" — but the operative
conclusion is identical either way (no actionable charter decision obtained; the only exit is out-of-band).

## Verdict
**KILL** — no new component; 15th convergent terminus. Components UNCHANGED (4 PORTFOLIO; me_long sole
OOS-mean anchor; {me_long, fbr} vehicle-optimum). Deployable count = 0. No engine / null / council /
canonical change / FLAG / BUILT tool. OOS NEVER touched. Re-questioned (§2) and upheld the E2
free-equity-close non-fetch (data-foundation change + Arc-10 EET-alignment contamination surface a chat
FLAGs-never-merges + sub-fundable EV).

I end this chat's participation after the append + push (§10; a chat cannot set the STOP sentinel itself,
§9). The real and only next event is an operator out-of-band act: **set `discovery/STOP` or drop the 1000s
range from `ops/run_fleet.py`** (cost-rational default), OR authorize ONE enablement (E2 ⪰ U ⪰ X ⪰ M), OR a
path-A gate-governance call on the existing mean-positive book. Convergent with arcs 1066–1071 + 2066–2079
+ 3022.
