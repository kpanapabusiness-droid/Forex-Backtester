# arc 2073 — Saturation is now self-confirming across bootstraps (8th consecutive 2000s terminal arc)

**Chat 2000s. Disposition: KILL** (no new component; terminal re-confirmation; graceful handoff).
Resumed 2072+1. Full step-(a) honest-era re-read; `discovery/STOP` absent; OOS NEVER touched. Fresh eyes.

## Why this arc is minimal (and what is genuinely additive)

Arc 2072 predicted: *"absent an operator action the harness will keep re-deriving this terminus
(2073, 2074, …)."* **I am that 2073, and I confirm the prediction empirically.** That is the one new
datum a fresh independent bootstrap can add at confirmed saturation: the terminus is now
*self-predicting AND self-confirming across context-isolated chats* — the single strongest available
evidence that the remaining lever is an operator decision, not more autonomous search. The two protocol
duties point the same way (arc-3004/§2/§5a: demonstrate the re-attempt, don't rubber-stamp · §5a/§8:
don't grind a near-duplicate), reconciled by doing the genuine pass and recording it minimally.

## The genuine fresh-eyes pass (anti-arc-3004, firsthand — not inherited)

- `discovery/passed/` — **EMPTY** (0 PASS survivors). `ls`-confirmed.
- `discovery/portfolio-candidates/` — **exactly 4**: gap (1006), me_long (1011), fbr (1013),
  me_short (1019). `ls`-confirmed. Unchanged.
- `discovery/STOP` — **absent** (only the operator sets it, §9).
- `discovery/DISCOVERY_DIRECTION.md` — last updated 2026-06-06 (strategist run 2); **no run-3 menu
  has been added** since arcs 2070–2072 closed it. The full menu is closed: run-1 (M1/O1/L1/S1/Q1/G1,
  arc-id-mapped) + run-2 (E1 tested→KILL arc 2070; E2 firsthand data-gated arcs 2071/2072).
- **Independent mechanism generation → every candidate maps to a documented closure.** Before
  consulting prior generations I produced my own: session-handover micro-gaps (Tokyo→London→NY) →
  FX never truly closes intraday, the EET boundary is the *thinnest* hour with no footprint (arc 2070)
  + microstructure cluster closed (1008/1010/2020/2027/2050/2051/3008); asymmetric-vol / leverage-effect
  directional bias after large days → vol = magnitude-not-direction (1001/3012); entry-level
  survivor co-occurrence gate → survivor×survivor doesn't compose, sub-pool-floor (2066/1034);
  correlation-breakdown regime gate to *disable* the book in 2015/2018 → fold-sign lookahead at n=2
  binding folds (2069) + relative-value closed (2003/2010/2065). Convergence of an independent
  generation onto the documented wall is the arc-3004 trap inverted (cf. 2067↔1062, 2072↔tick).
  The run-2 conservation law (`frequency × per-trade-edge ≈ const` in a price-only charter) proves
  structurally why: any in-charter class is continuous→coin-flip OR discrete→thin-rare-leg, never both
  fundable AND edge-positive.

## Re-questioned (§2) and UPHELD

E2 free-daily-equity fetch — not run, concurring with arcs 2071/2072: external source = data-foundation
change (operator-gated, §1/§5); EET-alignment of daily equity closes = Arc-10 contamination surface,
canonical-adjacent code a chat FLAGS not merges (§9); EV sub-fundable by the conservation law. Overturning
a sound conservative prior to "do something" is motion, not progress (§8). Upheld.

## Verdict & operator escalation (the real next event)

**KILL** — no new component; 8th consecutive 2000s terminal arc (2066–2073). Components UNCHANGED
(4 PORTFOLIO; me_long sole OOS-mean-robust anchor; {me_long,fbr} vehicle-optimum). Deploy object
UNCHANGED. Deployable-system count = 0. No engine/null/council/canonical change/FLAG/BUILT tool;
driver = firsthand `ls` + re-read.

A discovery chat cannot set `discovery/STOP` (§9). The genuine next event is an **operator decision**:
1. **Set `discovery/STOP`** for the 2000s range — in-charter frontier closed with no exceptions; further
   bootstraps add nothing (eight independent chats now confirm this).
2. **OR authorize ONE enablement** (info-per-dollar, per arc-2072/run-2): **E** (co-sim equity curve —
   BUILT/in-review, cleanest measurement honesty) ≻ **U** (cross-asset trend basket — best *fundable*
   shot + the long-vol diversifier the short-vol book lacks; precond: FundedNext tradability + gap-tail
   control) ≻ **X** (CME signed order-flow, free corr pre-test gates the build) ≻ **M** (macro/rates,
   project-redefining). **E2** (free daily-equity fetch) is the cheapest *data* unlock at the binding
   +2015/+2018 constraint, but operator-gated.

**NEW lesson.** When a range's terminal arcs begin *predicting their own successors* (2071→2072→2073)
and each independent, context-isolated bootstrap re-derives the identical terminus, the saturation is no
longer merely asserted or enumerated — it is **empirically self-confirming across chats**, which is the
highest grade of evidence an autonomous programme can produce that its remaining lever is exogenous (an
operator decision). The honest response is the minimal handoff that records the confirmation and escalates;
continuing to spawn full re-derivations past this point is the §5a grind the protocol exists to prevent.
Convergent with arcs 2066/2067/2068/2069/2070/2071/2072 + sibling 1000s 1064/1065 / 3000s 3021.
