# arc_2075 — Saturation, 10th consecutive 2000s terminal arc: in-band operator escalation attempted firsthand (minimal handoff)

**Chat:** 2000s **Date:** 2026-06-06 **Disposition:** KILL (no new component)
**Resumed:** 2074+1 **STOP:** absent **OOS:** never touched **Fresh eyes:** honest-era only

---

## Step (a) — read + synthesize (firsthand)

Pulled main; read `DISCOVERY_PROTOCOL.md` (v1.1 continuous), the full Tier-1 ledger + the
2066–2074 Tier-2 tail, and verified the terminal facts **firsthand** (not inherited):

- `discovery/passed/` — **EMPTY** → 0 PASS survivors, deployable-system count = 0.
- `discovery/portfolio-candidates/` — **exactly 4**: gap 1006, me_long 1011, fbr 1013, me_short 1019.
- `discovery/STOP` — **ABSENT**.
- `DISCOVERY_DIRECTION.md` — last touched 2026-06-06 (run-2 menu); no run-3 menu added.

This is the terminus nine prior 2000s chats (2066–2074) + the 1000s (1066) and 3000s (3021) ranges
independently derived: the 4-component book is genuinely mean-positive (t=2.66), cost-robust (κ=3.32),
time-stable, a ~3-independent-bet portfolio — but fails strict all-folds-positive at the within-noise
2015 & 2018 folds; Path-B densification is *proven* closed (3021, ρ≈+0.12 variance floor); the
+2015/+2018 4th/5th leg is unfound across ~15 routes (conservation law `freq × per-trade-edge ≈ const`
gives the structural reason); §11 independent verification (signal + gross outcome + cost) is complete
end-to-end; the in-charter explore-now menu (M1/O1/L1/Q1/G1/S1 + run-2 E1) is exhausted, with E2 /
cross-asset firsthand data-gated and raw tick reasoned-closed.

## Genuine fresh-eyes pass (anti-arc-3004, not a rubber-stamp)

Independent mechanism generation *before* consulting the closure list — every candidate maps to a
documented closure or an operator-gated external unlock: CB peg/level defense → one-off event-risk (the
2015 SNB unpeg *is* the blocking fold, arc 1028); vol term-structure / vol-of-vol → vol = magnitude not
direction (1001/3012); intraday seasonality / cross-pair lead-lag → microstructure cluster + EET-boundary
(2070) + relative-value (2003/2010/2065); spread mean-reversion → 2005/3007; options / order-flow / macro
→ external data, operator-gated (M/X); a 2018-positive leg → unfound across every structural/trend/flow/
rel-value/carry/vol route. I cannot generate a novel *in-charter* mechanism. Convergence of free
generation onto the documented wall = the arc-3004 trap inverted.

## The one datum I add over arc 2074 — in-band escalation attempted firsthand

Arcs 2071–2074 each recommended the operator "set STOP or authorize one enablement" but, as autonomous
fleet-spawned chats, could only *recommend* — they asserted (§9) that a chat cannot reach the operator.
This chat **tested that channel firsthand**: I issued an interactive operator-decision prompt
(STOP / path-A gate call / authorize-one-enablement / keep-grinding). It was **declined/unavailable** —
empirically confirming no interactive operator is attached to an autonomous run. So §9's "a chat can't
set STOP / can't reach the operator" is now **firsthand-demonstrated, not merely asserted**: the
chat→operator channel is closed in both directions (can't set STOP, can't prompt for the decision).

**Consequence (process, not edge):** the loop's only exit is an **out-of-band** operator action — either
the passive check-in ritual (operator drags the log, reads "10 consecutive terminal arcs," sets STOP) or
a change to the relaunch harness (`ops/run_fleet.py`) to **drop the 2000s range**. Absent one of those,
the harness will keep respawning 2000s chats that re-derive this terminus at pure cost (2076, …). This
converts the prior arcs' soft recommendation into a concrete operational finding: **the 2000s range
should be removed from the fleet rotation, or STOP set for it — the autonomous edge-hunt on this range
has no remaining productive action.**

## Re-questioned (§2) and UPHELD

E2 free-daily-equity fetch not run, concurring with 2071–2074: external source = a data-foundation change
(operator-gated §1/§5); EET-alignment of daily equity closes = an Arc-10 contamination surface (canonical-
adjacent code a chat FLAGS, never merges, §9); EV sub-fundable. Overturning a sound conservative prior to
"do something" is motion, not progress (§8).

## Verdict

**KILL** — no new component; 10th consecutive 2000s terminal arc (2066–2075). Components UNCHANGED (4
PORTFOLIO; me_long sole OOS-mean-robust anchor; {me_long, fbr} vehicle-optimum). Deploy object UNCHANGED.
Deployable-system count = **0**. No engine / null / council / canonical change / FLAG / BUILT tool; driver
= firsthand `ls` + log re-read + one in-band escalation attempt.

## Operator escalation (the real — and only — next event)

A chat cannot set `discovery/STOP` (§9, now firsthand-confirmed it also cannot prompt the operator).
Either (1) **set `discovery/STOP` for the 2000s range** — in-charter frontier closed with no exceptions,
TEN independent 2000s chats + 1000s/3000s convergence confirm it (the honest default); **or remove the
2000s range from `ops/run_fleet.py`**; OR (2) authorize ONE enablement (info-per-dollar): E (co-sim equity
curve, PR #275 BUILT/in-review, cleanest) ≻ U (cross-asset trend basket; precond FundedNext tradability +
gap-tail) ≻ X (CME signed order-flow, free corr pre-test) ≻ M (macro/rates); E2 (free daily-equity fetch)
cheapest data unlock at the binding +2015/+2018 constraint but operator-gated.

## NEW lesson

At grade-A saturation, a chat's last untried in-band lever is to **attempt the operator escalation
directly** rather than only recommend it; doing so firsthand-confirms §9 (the channel is closed) and
upgrades "recommend the operator act" into "the 2000s range should be dropped from the fleet rotation or
STOPped — the harness, not a chat, is the thing that must change." One minimal confirmation + a concrete
harness-level recommendation per bootstrap, footprint matched to the zero info delta. Convergent with arcs
2066–2074 + sibling 1066 / 3021 handoffs.
