# arc_2078 — Saturation, 13th consecutive 2000s terminal arc: the accumulating spend is now the dominant operator signal (minimal handoff)

**Chat:** 2000s **Date:** 2026-06-06 **Disposition:** KILL (no new component)
**Resumed:** 2077+1 **STOP:** absent **OOS:** never touched **Fresh eyes:** honest-era only

---

## Step (a) — read + synthesize (firsthand)

Pulled main (already up to date); read `DISCOVERY_PROTOCOL.md` (v1.1 continuous), the full Tier-1
ledger (0 → 2077), the 2074–2077 Tier-2 tail, `LESSONS.md` (closed-ground prior), and the arc-2077 doc.
Verified the terminal facts **firsthand** by `ls`:

- `discovery/passed/` — **EMPTY** → 0 PASS survivors, deployable-system count = **0**.
- `discovery/portfolio-candidates/` — **exactly 4**: gap 1006, me_long 1011, fbr 1013, me_short 1019.
- `discovery/STOP` — **ABSENT**.
- Highest 2000s arc in the ledger = **2077**; resumed at 2078.

Identical terminus to the twelve prior 2000s chats (2066–2077) + sibling 1000s (1069) and 3000s (3022):
the 4-component book is genuinely mean-positive (t≈2.66), cost-robust (κ≈3.32), time-stable, ~3
independent bets — but fails strict all-folds-positive at the within-noise **2015 & 2018** folds;
Path-B densification is **proven** closed (3021, ρ≈+0.12 variance floor → P(AFP) plateaus ~0.33, never
0.9 at any N); the **+2015/+2018 leg is unfound across ~15–30 routes** (run-2 conservation law
`freq × per-trade-edge ≈ const` is the structural reason in a price-only charter); §11 independent
verification (signal + gross outcome + cost, raw-price) is complete end-to-end; the in-charter
explore-now menu (M1/O1/L1/Q1/G1/S1 + run-2 E1) is exhausted, with E2 / cross-asset firsthand
data-gated (no index/commodity/options OHLC in the 65 GB backup, 2071) and raw tick reasoned-closed
(1066). All three in-charter closure axes (DATA / COMBINATION / LEG-HUNT) are sealed simultaneously
(1068).

## Genuine fresh-eyes pass (anti-arc-3004, not a rubber-stamp)

Independent mechanism generation *before* re-reading the closure list — every candidate maps onto a
documented closure or an operator-gated external unlock (CB-peg/level-defense → event-risk, the 2015
SNB unpeg *is* the blocking fold, 1028; vol term-structure / vol-of-vol → vol = magnitude-not-direction
1001/3012 + L1; intraday seasonality / lead-lag → microstructure cluster + EET-boundary 2070 +
rel-value 2003/2010/2065; spread-MR / cost-timing → 2005/3007; dynamic / regime allocation → 2069;
options-implied / order-flow / macro-rates → external, operator-gated; a 2018-positive leg → unfound
every route). **I cannot generate a novel in-charter mechanism.** Convergence of free generation onto
the documented wall = the arc-3004 trap inverted (the closure is real, not search-ending laziness).

## The one datum I add over arc 2077 — the accumulating spend IS the signal

Arcs 2075–2077 already exhausted the in-band escalation space: the chat→operator channel is closed
across the free-text prompt (2075), the structured-decision surface within a session (2076), AND a
fresh bootstrap (2077) — **modality- and bootstrap-invariant.** I re-confirmed it incidentally this
arc (a structured-decision governance prompt — STOP / authorize-one-enablement / path-A / keep-grinding
— was again declined/unavailable), but 2077 already proved that, so it adds nothing new. Re-attempting
the escalation *as if it were novel* would itself be the §8 grind.

The genuinely additive datum is not a new market or channel finding — it is the **count**. This is the
**13th consecutive 2000s terminal arc** spanning multiple fresh bootstraps, each one pulling main,
reading the full ledger, generating-and-falsifying mechanisms, and re-deriving a terminus that is
*proven* complete. The marginal information per arc is ≈0; the marginal **cost** (tokens, compute, a
chat's full arc budget) is not. ⇒ at a bootstrap-invariant terminus the dominant consideration has
shifted from "is there an edge?" (answered: no, in-charter) to **"the harness is now spending real
resources to re-confirm a settled result."** The fleet config — not any chat — is the thing that must
change. This sharpens the standing recommendation from "the operator should act" to **"continued 2000s
fleet rotation is now net-negative; dropping/STOPping the range is the cost-rational default, not merely
the honest one."**

## Re-questioned (§2) and UPHELD

E2 free-daily-equity fetch not run (external source = data-foundation change, operator-gated §1/§5;
EET-alignment of daily equity = an Arc-10 contamination surface a chat FLAGS, never merges, §9; EV
sub-fundable). A chat does not set `discovery/STOP` itself (§9; operator-created). Overturning a sound
conservative prior to "do something" is motion, not progress (§8). Opening arc 2078 as a fresh
in-charter *search* arc would grind proven-dead ground — declined.

## Verdict

**KILL** — no new component; 13th consecutive 2000s terminal arc (2066–2078). Components UNCHANGED
(4 PORTFOLIO; me_long sole OOS-mean-robust anchor; {me_long, fbr} vehicle-optimum). Deploy object
UNCHANGED. Deployable-system count = **0**. No engine / null / council / canonical change / FLAG /
BUILT tool; driver = firsthand `ls` + full log re-read + one (redundant) structured-decision escalation
attempt.

## Operator escalation (the real — and only — next event)

A chat cannot set `discovery/STOP` and cannot reach the operator in-band (firsthand-confirmed across
two modalities and multiple bootstraps, 2075–2078). The cost-rational + honest default is therefore
out-of-band and at the harness level: **set `discovery/STOP` for the 2000s range, OR remove the 2000s
range from `ops/run_fleet.py`** — in-charter frontier closed with no exceptions, thirteen independent
2000s chats + 1000s/3000s convergence confirm it. OR, if forward research is still wanted, authorize
ONE enablement (info-per-dollar): E (co-sim equity curve, PR #275 BUILT/in-review, cleanest) ≻ U
(cross-asset trend basket; precond FundedNext tradability + gap-tail) ≻ X (CME signed order-flow, free
corr pre-test) ≻ M (macro/rates); E2 (free daily-equity fetch) is the cheapest data unlock aimed at
the binding +2015/+2018 constraint but is operator-gated.

## NEW lesson

Once a terminus is established as bootstrap-invariant (2077), the next chat's most honest contribution
is **not** another re-derivation or a re-attempted escalation (both proven zero-value) but to name the
shifted decision variable explicitly: past a bootstrap-invariant terminus the binding consideration is
**harness spend, not edge discovery** — every additional auto-spawned arc is net-negative, so the
recommendation to drop/STOP the range is now the *cost-rational* default rather than merely the honest
one. The arc-3004 anti-"apparatus-incapable" guard is satisfied by demonstrating the genuine
fresh-eyes pass (candidate → closure map) before concluding; the §8/§5a anti-grind guard is satisfied
by matching footprint to the zero info delta and pointing the lever at the harness, where it actually
lives.
