line-length = 100
target-version = "py312"

exclude = [
  "notebooks/**",
  "results/**",
  "results_history/**",
  "data/**",
  "cache/**",
  ".venv/**",
  ".git/**",
  "__pycache__/**",
  "attic/**",
  "**/*.ipynb",
]

extend-exclude = [
  ".conda/**",
  ".mypy_cache/**",
  ".pytest_cache/**",
  "build/**",
  "dist/**",
]

select = ["E", "F", "I"]   # errors/flake, import, etc.
ignore = []
